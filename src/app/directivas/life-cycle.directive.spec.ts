import { LifeCycleDirective } from './life-cycle.directive';

describe('LifeCycleDirective', () => {
  it('should create an instance', () => {
    const directive = new LifeCycleDirective();
    expect(directive).toBeTruthy();
  });
});
